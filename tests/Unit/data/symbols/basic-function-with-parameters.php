<?php

/**
 * A function that does a thing, and has gained a couple of parameters along the way.
 *
 * @since 1.2.0
 * @since 1.3.0 Added the `$more` parameter.
 * @since 2.0.0 Added the `$extra` parameter.
 *
 * @param string $thing The thing.
 * @param bool   $more  Optional. Whether to do more. Default false.
 * @param array  $extra Optional. Extra data for the thing. Default empty array.
 * @return bool Whether the thing was done.
 */
function wpcompat_test_function_with_parameters( $thing, $more = false, $extra = array() ) {}
